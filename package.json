{
  "name": "4ever-aos",
  "version": "1.0.0",
  "private": true,
  "description": "Commitlint + Husky setup for AOS",
  "scripts": {
    "prepare": "husky install",
    "lint:commit": "commitlint --from=HEAD~1 --to=HEAD --format ./scripts/commitlint-formatter-korean.js"
  },
  "type": "commonjs",
  "devDependencies": {
    "@commitlint/cli": "^20.0.0",
    "@commitlint/config-conventional": "^20.0.0",
    "husky": "^8.0.0"
  }
}

